import type { Lesson, QuizQuestion } from '../types'

export const chapter2Lessons: Lesson[] = [
  {
    id: 'ch2-app-architectures', number: '01', title: 'Building Network Applications', eyebrow: 'Programs at the edge', minutes: 17,
    summary: 'Design a distributed application by deciding where its processes run and how client-server and peer-to-peer systems divide work.',
    objectives: ['Explain why application logic is normally deployed at end systems', 
      'Identify client and server roles from who initiates communication', 
      'Compare scaling, availability, and management in client-server and peer-to-peer architectures', 
      'Recognize that an application architecture is separate from the network architecture beneath it'],

    sections: [
      { title: 'Application logic belongs at the edge', 
        body: 'A network application is a collection of processes on end systems that exchange messages. Browsers, mail clients, game processes, and streaming servers use transport services, while routers in the core forward packets without understanding the application conversation. This separation lets developers deploy a new application by changing edge software rather than upgrading every router on its path.', 
        points: ['The application layer determines message meaning and behavior.', 
          'The transport layer carries messages between processes.', 
          'An application can evolve independently of the many networks it crosses.'] },

      { title: 'Client-server organization', 
        body: 'In a client-server design, a server process waits at a known address and clients initiate exchanges. A production service may present one logical server name while load balancers distribute requests across many replicated servers in data centers. Clients can disconnect, move between networks, and receive dynamic addresses because other clients do not need to locate them directly.', 
        points: ['Examples include Web browsing, e-mail access, and file transfer.', 
          'Centralization simplifies authentication, updates, backups, and policy enforcement.', 
          'Server capacity and availability can become bottlenecks unless the service is replicated.'] },

      { title: 'Peer-to-peer organization', 
        body: 'A peer-to-peer application lets arbitrary end systems exchange data directly without relying on one always-on service host for every transfer. During one exchange a peer may request a block as a client; during another it may serve that block. BitTorrent illustrates this role switching by distributing pieces among participating hosts.', 
        points: ['Self-scalability means a new peer contributes upload capacity as well as adding download demand.', 
          'Peer churn, dynamic addresses, firewalls, and unequal upload rates complicate discovery and coordination.', 
          'Peer-to-peer does not mean that no supporting server can exist; a tracker or sign-in service may assist while bulk data still moves peer to peer.'] },

      { title: 'Choose architecture from constraints', 
        body: 'Architecture is a tradeoff rather than a protocol property. A bank may prefer centrally controlled servers for consistency and auditing, while a large file swarm benefits from participant upload capacity. Hybrid systems combine central coordination with direct peer transfers. The right choice follows from trust, cost, traffic pattern, availability, and operational requirements.', 
        points: ['Ask who must be reachable at a stable address.', 
          'Ask where authoritative state should live.', 
          'Ask whether participating users can safely provide resources to one another.'] },
      { title: 'Beginner example: sharing a class video',
        body: 'Imagine thirty students need the same large class video. With client-server, every student asks the course server, so the school controls the official copy but must supply all upload traffic. With peer-to-peer, students who already have pieces can upload them to classmates, reducing dependence on one sender but making participation and trust harder to manage. A hybrid could use a school server to identify the video and peers while allowing direct piece transfers.',
        points: ['The application architecture says where the work happens; it does not change the Internet\'s packet-forwarding architecture.', 'Calling a process a client or server describes its role in an exchange, not necessarily the kind of computer it runs on.'] },
    ],
    keyTerms: ['network application', 'end system', 'application architecture', 'client-server', 'client', 'server', 'data center', 'peer-to-peer', 'self-scalability', 'peer churn'], source: 'Chapter 2 slides 3-7',
  },
  {
    id: 'ch2-processes-sockets', number: '02', title: 'Processes, Sockets, and Protocols', eyebrow: 'Finding the right program', minutes: 19,
    summary: 'Trace an application message between processes through sockets and identify both the destination host and destination service.',
    objectives: ['Distinguish a host, process, and socket', 
      'Determine client and server process roles for an exchange', 
      'Construct a process endpoint from an IP address and port number', 
      'Explain protocol message types, syntax, semantics, and sequencing', 
      'Contrast open and proprietary protocols'],
    sections: [
      { title: 'The communicating entities are processes', 
        body: 'A process is a running program. Processes on one host can use operating-system inter-process communication, but processes on different hosts exchange network messages. The process that initiates a particular conversation is the client process; the process waiting to be contacted is the server process. These are behavioral roles, so even two identical peer programs take opposite roles in a given exchange.' },

      { title: 'A socket is the application boundary', 
        body: 'A socket is the interface between an application process and transport services. The developer controls message content and when the process reads or writes. The operating system handles transport buffers, segmentation, addressing, and packet delivery below that interface. End-to-end communication therefore has at least one socket at each endpoint.', 
        points: ['Writing to a socket does not mean the remote process has already received the data.', 
          'A TCP socket exposes a reliable byte stream; a UDP socket exposes individual datagrams.', 
          'The socket API is an interface, not an application-layer protocol.'] },

      { title: 'Host address plus process address', 
        body: 'An IP address routes traffic to a host interface, but a host may run a browser, Web server, mail server, and many other processes simultaneously. A transport-layer port number selects the intended service or socket on that host. The pair is often written as IP:port, such as 128.119.245.12:80 for the deck\'s Web-server example.', 
        points: ['Servers commonly listen on well-known ports such as 80 for HTTP and 443 for HTTPS.', 
          'Clients normally use temporary source ports chosen by the operating system.', 'Both source and destination endpoint information allows replies to return to the correct client process.'] },
      { title: 'A protocol defines a conversation', 
        body: 'An application-layer protocol specifies message types, their syntax, field semantics, and the rules governing when messages are sent and how recipients respond. For HTTP, request and response are message types; a request line is syntax; GET expresses retrieval semantics; and sending a response after a request is part of the exchange rule. Every part is needed for independent implementations to communicate correctly.' },
      { title: 'Open versus proprietary', 
        body: 'An open protocol is publicly specified, commonly in an RFC, so independently written clients and servers can interoperate. A proprietary protocol is controlled by an organization and may require permission or reverse engineering to implement. Open describes access to the specification, not whether a particular implementation is free, secure, or well designed.' },
      { title: 'Beginner example: delivering one browser request',
        body: 'Suppose a browser process sends a request to 203.0.113.8:443. The IP address identifies the destination host, port 443 identifies the server process, and the browser writes application bytes through its socket. The server operating system delivers those bytes to the matching socket, where the server process interprets them according to its application protocol. Each label answers a different question: which machine, which process, and what the bytes mean.',
        points: ['A socket is like a process\'s doorway to transport service, not the route through the network.', 'The client also has an IP address and temporary port so the returning data reaches the correct browser connection.'] },
    ],
    keyTerms: ['process', 'inter-process communication', 'client process', 'server process', 'socket', 'socket API', 'IP address', 'port number', 'endpoint', 'application-layer protocol', 'syntax', 'semantics', 'RFC', 'interoperability'], source: 'Chapter 2 slides 8-12',
  },
  {
    id: 'ch2-transport-services', number: '03', title: 'Choosing a Transport Service', eyebrow: 'Application requirements first', minutes: 20,
    summary: 'Translate an application\'s reliability, delay, throughput, and security needs into a reasoned choice among TCP, UDP, and security layers.',
    objectives: ['Classify requirements for loss, delay, throughput, and security', 'Differentiate flow control from congestion control', 'Compare the service models of TCP and UDP', 'Explain why applications still choose UDP', 'State what TLS adds and what it does not replace'],
    sections: [
      { title: 'Start with requirements, not a favorite protocol', body: 'A correct transport choice follows from application behavior. A file must arrive exactly, but a live call may prefer one missing audio sample over a late retransmission. Interactive games are sensitive to tens of milliseconds, while an e-mail can wait. Video needs enough sustained throughput for its encoding rate, whereas an elastic download can simply take longer.', points: ['Reliability asks whether loss or reordering is acceptable.', 'Timing asks how much end-to-end delay and delay variation can be tolerated.', 'Throughput asks whether a minimum useful rate exists.', 'Security asks for confidentiality, integrity, and peer authentication.'] },
      { title: 'TCP service model', body: 'TCP first establishes a logical connection and then provides an ordered, reliable byte stream between processes. Lost bytes are retransmitted and duplicates are removed. Flow control protects the receiving process by limiting data in flight when its receive buffer fills. Congestion control protects the network by reducing the sending rate when network conditions indicate overload.', points: ['Reliability can increase delay because recovery takes time.', 'TCP preserves byte order but does not preserve application message boundaries.', 'TCP promises neither a maximum delay nor a minimum throughput.', 'Plain TCP does not encrypt or authenticate application data.'] },
      { title: 'UDP service model', body: 'UDP sends independent datagrams without connection establishment, retransmission, ordering, built-in flow control, or built-in congestion control. Its value is control: an application can preserve message boundaries, avoid TCP setup, tolerate selected losses, and implement only the recovery or timing behavior it needs. DNS queries, real-time media, and modern transports built in user space are common motivations.', points: ['Unreliable means delivery is not guaranteed, not that delivery always fails.', 'Applications using UDP still should behave responsibly under congestion.', 'If an application needs reliability over UDP, it must supply that behavior itself or use a protocol that does.'] },
      { title: 'What TLS contributes', body: 'Transport Layer Security is used by applications to protect a connection. A TLS handshake authenticates the server in the usual Web case and establishes keys; subsequent records provide confidentiality and integrity. In the chapter model, a TLS library runs above TCP, so TCP still supplies reliable transport while TLS protects the bytes. TLS does not create a bandwidth or latency guarantee.', points: ['HTTPS is HTTP carried through TLS rather than a different set of HTTP methods.', 'Authentication depends on certificate validation and trust anchors.', 'Encryption hides content, but endpoints and traffic patterns may still expose metadata.'] },
      { title: 'Worked decision', body: 'Suppose a backup program requires every byte, can use any available rate, and has no strict deadline. TCP matches its reliability and elastic-throughput needs; TLS should be added if the backup crosses an untrusted network. For a live game position update that becomes obsolete after 50 ms, UDP may be preferable because waiting for an old update can be worse than dropping it.' },
      { title: 'A simple requirements checklist', body: 'Before selecting a transport, write down what failure would hurt the application. A downloaded program cannot tolerate a changed byte, a voice call cannot tolerate long pauses, and a sensor update may become useless when the next update arrives. Then map those needs to transport behavior instead of assuming that one protocol is universally faster or better.', points: ['Need every byte in order: start with TCP.', 'Need control over retransmission and message timing: consider UDP plus application logic.', 'Need confidentiality and peer authentication: add an appropriate security protocol; neither TCP nor UDP supplies them by itself.'] },
    ],
    keyTerms: ['reliable data transfer', 'loss-tolerant', 'timing', 'throughput', 'elastic application', 'TCP', 'UDP', 'connection-oriented', 'flow control', 'congestion control', 'datagram', 'TLS', 'confidentiality', 'authentication'], source: 'Chapter 2 slides 13-17',
  },
  {
    id: 'ch2-http-connections', number: '04', title: 'Web Objects and HTTP Connections', eyebrow: 'Fetching a page', minutes: 23,
    summary: 'Decompose a page into separately addressed objects, trace a browser request, and calculate the latency cost of HTTP connection strategies.',
    objectives: ['Separate a Web page, base HTML file, and referenced objects', 'Identify the host and path portions of a URL', 'Trace HTTP over a TCP connection', 'Calculate sequential non-persistent response time', 'Explain how persistence, pipelining, and parallelism affect latency'],
    sections: [
      { title: 'A page is assembled from objects', body: 'A Web page is not normally transferred as one indivisible file. The browser first retrieves a base HTML object, parses it, and discovers URLs for images, style sheets, scripts, fonts, audio, or video. Each URL names a host and a path, and referenced objects may be on different servers. Consequently, displaying one page can require many request-response exchanges.' },
      { title: 'HTTP uses a request-response model', body: 'The browser is the HTTP client and the Web server is the HTTP server. For classic HTTP over TCP, the client connects to the server, sends a request through its socket, receives a response, and eventually closes or reuses the connection. HTTP is stateless: each request can be understood without requiring the protocol to remember earlier requests. Applications can still build state above HTTP with cookies or other identifiers.' },
      { title: 'Non-persistent HTTP timeline', body: 'Non-persistent HTTP carries at most one object per TCP connection. In the simplified chapter model, fetching one small object costs one RTT for TCP setup, one RTT for the request and first response bytes, plus object transmission time L/R. The formula is response time = 2RTT + L/R, excluding DNS lookup, queueing, processing, and connection teardown.', points: ['If RTT = 80 ms and a 400,000-bit object crosses a 10 Mbps bottleneck, L/R = 0.04 s = 40 ms.', 'Total modeled time is 2(80 ms) + 40 ms = 200 ms.', 'RTT is propagation and protocol travel time; L/R is the time to place all object bits on the bottleneck link.'] },
      { title: 'A multi-object calculation', body: 'Assume one base HTML file references ten images, all fetched sequentially from the same server with non-persistent HTTP, and ignore transmission time. There are eleven objects, each costing two RTTs, so the page costs 11 x 2RTT = 22RTT. With a 100 ms RTT that is 2.2 seconds before adding object transmission and other delays. Parallel connections can overlap some waits but consume more connection and server resources.' },
      { title: 'Persistent HTTP reuses setup', body: 'Persistent HTTP keeps one TCP connection available for multiple objects from the same server. The browser pays connection setup once, avoids repeated operating-system connection overhead, and can issue later requests as objects are discovered. With pipelining in the chapter\'s simplified HTTP/1.1 model, referenced requests may share one additional RTT rather than each paying two RTTs. This is a latency model, not a promise that all object bytes arrive instantly.' },
      { title: 'Beginner example: count objects before RTTs', body: 'If index.html references logo.png and site.css, the page contains three separately requested objects. Under sequential non-persistent HTTP and the simplified model, each costs two RTTs plus its own transmission time. Under a persistent connection, TCP setup can be reused. The useful habit is to count objects and connections first, then apply the timing assumptions stated in the problem.', points: ['A URL in HTML causes another request; it does not place that object inside the HTML file.', 'Objects on a different host need a connection to that other host.', 'DNS time is excluded only when the problem explicitly says to ignore it.'] },
    ],
    keyTerms: ['Web page', 'base HTML', 'Web object', 'URL', 'host name', 'path', 'HTTP', 'stateless', 'non-persistent HTTP', 'persistent HTTP', 'pipelining', 'parallel connection', 'RTT', 'transmission time'], source: 'Chapter 2 slides 18-26',
  },
  {
    id: 'ch2-http-messages', number: '05', title: 'Reading HTTP Messages', eyebrow: 'Requests and responses', minutes: 21,
    summary: 'Read raw HTTP request and response messages by locating start lines, headers, delimiters, bodies, methods, and status codes.',
    objectives: ['Parse the start line, headers, blank line, and optional body', 'Interpret common request headers', 'Compare GET, POST, HEAD, and PUT semantics', 'Interpret representative 2xx, 3xx, 4xx, and 5xx responses', 'Match response metadata to its purpose'],
    sections: [
      { title: 'Request anatomy', body: 'An HTTP/1.1 request begins with a request line containing method, request target, and version, for example GET /notes.html HTTP/1.1. Header fields follow as name-value lines. A blank CRLF line terminates the header section, after which an optional message body may appear. The Host header identifies the intended virtual host; Accept describes usable media types; Accept-Encoding can advertise compression; and Connection historically influenced persistence.' },
      { title: 'Read a request as evidence', body: 'Consider GET /search?q=udp HTTP/1.1 followed by Host: example.edu and a blank line. GET states the action, /search?q=udp identifies the target and query data, HTTP/1.1 selects message rules, and Host identifies the site at the destination server. There is no request body in this example. CRLF is the two-character carriage-return/line-feed line ending used by HTTP/1.x framing.' },
      { title: 'Methods express intent', body: 'GET requests a representation and may encode parameters in the query string. POST submits data in the request body for the target to process. HEAD asks for the metadata a GET would return but omits the response body, making it useful for checking size or modification information. PUT stores the enclosed representation at the named target, creating or completely replacing it according to server policy.', points: ['A body is not synonymous with POST; PUT also commonly carries one.', 'A query string is visible in the URL and should not be treated as secret.', 'Method semantics describe intent; authorization still determines whether an operation is allowed.'] },
      { title: 'Response anatomy', body: 'An HTTP/1.1 response starts with a status line such as HTTP/1.1 200 OK. Headers then describe the response and representation: Content-Type identifies the media format, Content-Length gives body size when known, Last-Modified gives a validator date, and Location supplies a redirect target. A blank line separates headers from an optional body containing HTML, an image, an error document, or other data.' },
      { title: 'Status codes are grouped', body: 'The first digit identifies the broad outcome: 2xx success, 3xx redirection, 4xx client-side request problems, and 5xx server-side failure. In this chapter, 200 OK carries a successful representation, 301 Moved Permanently points to a new Location, 400 Bad Request means the server could not understand the request, 404 Not Found means no matching resource was found, and 505 means the HTTP version is unsupported. The numeric code, not the human-readable phrase, drives client behavior.' },
      { title: 'Worked message reading', body: 'For POST /login HTTP/1.1, Host: example.edu, Content-Length: 14, a blank line, and then user=ada&pin=7, read from the outside inward. POST is the method, /login is the target, the headers describe handling and body length, and the bytes after the blank line are the submitted representation. A response beginning HTTP/1.1 404 Not Found has the same broad structure but starts with a status line instead of a request line.', points: ['Do not confuse the Host header with the path in the request line.', 'Content-Length counts body bytes, not header lines.', 'A response can include an explanatory body even when its status reports an error.'] },
    ],
    keyTerms: ['request line', 'request target', 'status line', 'header field', 'CRLF', 'message body', 'Host', 'Content-Type', 'Content-Length', 'GET', 'POST', 'HEAD', 'PUT', 'query string', 'status code', 'Location'], source: 'Chapter 2 slides 27-31',
  },
  {
    id: 'ch2-cookies-state', number: '06', title: 'Cookies, State, and Privacy', eyebrow: 'Remembering a stateless client', minutes: 22,
    summary: 'Follow a cookie identifier through browser storage and server state, then evaluate how the same mechanism enables sessions and tracking.',
    objectives: ['Reconcile stateless HTTP with stateful Web applications', 'Trace all four components of the chapter cookie model', 'Distinguish session data from a cookie identifier', 'Explain first-party and cross-site tracking flows', 'Connect identifying cookies to privacy regulation and user control'],
    sections: [
      { title: 'Stateless protocol, stateful application', body: 'HTTP being stateless means one request does not inherently depend on protocol memory of a previous request. A shopping cart is nevertheless stateful at the application level. The site can place a small identifier in the browser and use it as a key into server-side records. This separation keeps each HTTP message self-describing enough to process while allowing a sequence of requests to belong to one user session.' },
      { title: 'The four-part cookie flow', body: 'On a first visit, the server creates an identifier and a database record, then returns Set-Cookie: session=K7 in an HTTP response. The browser stores the cookie under rules for its site. A later matching request includes Cookie: session=K7. The server uses K7 to retrieve the cart, login, preferences, or other state from its back-end database.', points: ['The response Set-Cookie header creates or updates browser state.', 'The later request Cookie header returns applicable name-value pairs.', 'Browser-managed storage persists the value for the allowed lifetime and scope.', 'The server database associates the opaque identifier with application state.'] },
      { title: 'Identifier is not the whole session', body: 'A cookie can contain data, but session cookies often contain only an unpredictable identifier. The sensitive cart or account record remains on the server. If an attacker steals a valid session identifier, the server may mistake the attacker for the user, so HTTPS, restrictive cookie attributes, expiration, and server-side invalidation are important even though those controls are beyond the slide sequence.' },
      { title: 'First party and third party', body: 'A first-party cookie belongs to the site represented in the browser address bar and can correlate activity within that site. If the page embeds an image, script, or ad from another domain, the browser also requests that resource. That third party can receive its own identifier plus contextual information and recognize the same browser when embedded elsewhere, building a cross-site history without a direct visit to the tracker.', points: ['The embedded resource can be visible or effectively invisible.', 'A shared identifier turns separate site visits into a linkable profile.', 'Browser privacy behavior changes over time, so the deck\'s named-browser blocking claim should be treated as a dated snapshot rather than an HTTP rule.'] },
      { title: 'Privacy and legal significance', body: 'A persistent identifier can become personal data when it identifies or helps profile a natural person, especially when combined with IP addresses, account data, or browsing history. The deck connects this point to the EU GDPR and emphasizes meaningful user control. Technical ability to store a cookie does not by itself establish consent or a lawful purpose.' },
      { title: 'Beginner example: cart versus tracking', body: 'A store can assign browser A the random identifier 8F2 and keep the cart items in a server record keyed by 8F2. Returning that identifier lets the store restore the cart, which is useful first-party state. If an advertising domain embedded by many unrelated sites receives one of its own stable identifiers on each visit, it can link those visits. The mechanism is similar, but the scope, purpose, and privacy consequences differ.', points: ['Deleting or expiring the identifier can break the link to server-side session state.', 'A random-looking value can still identify a browser session.', 'HTTPS protects a cookie in transit when configured correctly, but does not decide whether tracking is appropriate.'] },
    ],
    keyTerms: ['stateless protocol', 'application state', 'cookie', 'Set-Cookie', 'Cookie header', 'session identifier', 'back-end database', 'first-party cookie', 'third-party cookie', 'tracking cookie', 'cross-site tracking', 'GDPR', 'personal data'], source: 'Chapter 2 slides 32-41',
  },
  {
    id: 'ch2-caching-http-evolution', number: '07', title: 'Caching and Modern HTTP', eyebrow: 'Less traffic, less waiting', minutes: 28,
    summary: 'Model cache hits and misses, validate stale objects efficiently, and distinguish HTTP/1.1, HTTP/2, and HTTP/3 approaches to concurrency.',
    objectives: ['Trace hit and miss paths through a Web cache', 'Calculate offered rate, link utilization, and weighted average delay', 'Explain freshness and conditional validation', 'Identify application-level and transport-level head-of-line blocking', 'Compare HTTP/1.1 ordering, HTTP/2 multiplexing, and HTTP/3 streams'],
    sections: [
      { title: 'A cache has two protocol roles', body: 'A Web cache, or proxy, receives browser requests as a server. On a hit it returns a stored representation nearby. On a miss it becomes a client of the origin server, forwards a request, stores an allowable response, and relays it. Hits reduce user delay, origin load, and traffic over a constrained access link. Response directives such as Cache-Control determine whether and how long a response may be reused.' },
      { title: 'Establish the no-cache baseline', body: 'The chapter scenario has 15 requests per second and an average object size of 100,000 bits, so the offered data rate is 15 x 100,000 = 1,500,000 bits/s, or 1.50 Mbps. A 1.54 Mbps access link therefore has utilization 1.50/1.54 = 0.974, about 97%. Because arrivals are bursty, queueing delay rises sharply as utilization approaches one even though the average rate is barely below capacity.', points: ['The 1 Gbps LAN utilization is only 1.50/1000 = 0.0015.', 'The access link, not the LAN, is the bottleneck.', 'Utilization is dimensionless: traffic rate divided by service rate.'] },
      { title: 'Apply the hit rate', body: 'With hit rate h = 0.40, the miss rate is 1 - h = 0.60. Only misses cross the access link, so its offered rate becomes 0.60 x 1.50 = 0.90 Mbps. New utilization is 0.90/1.54 = 0.584, about 58%, which makes access-link queueing comparatively small in the example. The cache reduces demand rather than increasing physical link speed.' },
      { title: 'Compute expected delay', body: 'Average delay is a weighted mean: Davg = (1 - h)Dmiss + hDhit. Using Dmiss = 2.01 s, a representative local Dhit = 0.01 s, and h = 0.40 gives 0.60(2.01) + 0.40(0.01) = 1.210 s. Rounding a millisecond-scale hit delay toward zero produces the deck\'s approximate 1.2 s result.', points: ['Weights must sum to one.', 'A higher hit rate lowers both access-link utilization and average delay.', 'A cache hit still has local processing and LAN delay; it is small, not literally zero.'] },
      { title: 'Freshness and conditional GET', body: 'A stored response eventually may need validation. A client can send If-Modified-Since with the timestamp of its copy. If the representation has not changed, the origin returns 304 Not Modified without the object body; if it changed, the origin returns 200 OK with new content. Validation still uses a request and response, but avoids retransmitting the full object when the cached copy is current.' },
      { title: 'HTTP/1.1 and application-level blocking', body: 'Persistent HTTP/1.1 can pipeline requests on one TCP connection, but responses are serialized in request order in the chapter model. If a large object is first, small objects wait behind its bytes. This is application-level head-of-line blocking. Multiple parallel TCP connections can reduce the wait but add setup and resource costs.' },
      { title: 'HTTP/2 multiplexes frames', body: 'HTTP/2 retains familiar methods, status codes, and semantics while encoding messages as binary frames assigned to streams. A server can interleave frames from a large response with frames from smaller responses, so the small objects can finish first. This addresses HTTP/1.1 response-order blocking, but all streams still share one ordered TCP byte stream: loss of a TCP segment can temporarily stall delivery for every stream behind the missing bytes.' },
      { title: 'HTTP/3 changes the transport', body: 'HTTP/3 carries HTTP semantics over QUIC, which runs over UDP and integrates TLS security. QUIC provides reliable data within independent streams, so a lost packet containing one stream\'s data need not block delivery of data already received for other streams. Congestion control still coordinates the connection rather than granting unlimited independent rates; the key improvement is per-stream loss recovery and reduced transport-level head-of-line blocking.' },
      { title: 'Beginner example: fresh, stale, and validated', body: 'Suppose a cache stores notes.pdf for ten minutes. A request after two minutes can be a fresh hit and use the local copy immediately. After ten minutes the copy is stale, but not automatically wrong: the cache can validate it with the origin. A 304 response means reuse the stored body, while a 200 response with a new body replaces it.', points: ['Freshness avoids contacting the origin when reuse is allowed.', 'Validation contacts the origin but may avoid transferring the object body.', 'Caching and HTTP version are separate ideas: caches can participate in multiple HTTP versions.'] },
    ],
    keyTerms: ['Web cache', 'proxy server', 'origin server', 'cache hit', 'cache miss', 'cache hit rate', 'offered rate', 'access link utilization', 'bottleneck', 'weighted average delay', 'Cache-Control', 'conditional GET', 'If-Modified-Since', '304 Not Modified', 'head-of-line blocking', 'HTTP/1.1', 'HTTP/2', 'frame', 'stream', 'multiplexing', 'HTTP/3', 'QUIC'], source: 'Chapter 2 slides 42-53',
  },
  {
    id: 'ch2-email-smtp', number: '08', title: 'How Internet Email Moves', eyebrow: 'From outbox to mailbox', minutes: 25,
    summary: 'Trace an email through user agents and mail servers, read a basic SMTP exchange, and distinguish message transfer from message access.',
    objectives: ['Identify user agents, mail servers, outgoing queues, and recipient mailboxes', 'Trace a message from the sender through SMTP delivery to the recipient', 'Explain SMTP connection setup, command-response transfer, and closure over TCP', 'Distinguish SMTP envelope commands from the headers inside a mail message', 'Compare SMTP push with IMAP or HTTP-based message retrieval'],
    sections: [
      { title: 'The three main parts of email', body: 'A user agent is the mail application a person uses to compose, send, and read messages. A mail server keeps incoming messages in user mailboxes and holds not-yet-delivered outgoing messages in a queue. SMTP, the Simple Mail Transfer Protocol, moves messages toward a receiving mail server. These pieces separate the human interface from the always-available systems that perform delivery.', points: ['The sender and receiver do not need to be online at the same time.', 'A mailbox stores messages delivered for one user.', 'An outgoing queue lets a server retry when the destination is temporarily unreachable.'] },
      { title: 'Alice sends mail to Bob', body: 'Alice composes a message to bob@someschool.edu in her user agent. Her agent submits it to Alice\'s mail server, which queues it. The SMTP client process on Alice\'s server opens a TCP connection to the SMTP server process at Bob\'s server and sends the message. Bob\'s server stores the accepted message in Bob\'s mailbox; later, Bob\'s user agent retrieves and displays it.', points: ['In the server-to-server exchange, the sending mail server is the SMTP client because it initiates the connection.', 'The receiving mail server is the SMTP server even though both machines are commonly called mail servers.', 'SMTP delivery and Bob reading the message are separate steps.'] },
      { title: 'SMTP is a TCP command-response protocol', body: 'SMTP uses reliable TCP; the chapter model shows server-to-server SMTP at destination port 25. A session has a greeting or handshake, one or more message transfers, and closure. Commands are ASCII text and replies contain a numeric status code plus a phrase. A persistent connection can carry multiple messages rather than reconnecting for each one.', points: ['220 announces that the server is ready.', 'HELO introduces the sending side, and a successful server reply can be 250.', 'MAIL FROM identifies the envelope sender; RCPT TO identifies an envelope recipient.', 'DATA begins message content, and QUIT closes the SMTP conversation.'] },
      { title: 'Read a small SMTP exchange', body: 'After the server sends 220, the client sends HELO sender.example. The client then sends MAIL FROM:<alice@example>, RCPT TO:<bob@example>, and DATA, checking each reply before continuing. A 354 reply asks for the content. In the slide model, the client finishes that content with CRLF, a period on a line by itself, and CRLF; the server can then reply 250 to accept it for delivery.', points: ['The command order matters because each command changes the conversation state.', 'A status reply confirms protocol progress; it is not the email body.', 'SMTP is primarily push: the sending side initiates delivery toward the receiving server.'] },
      { title: 'Envelope versus message format', body: 'SMTP MAIL FROM and RCPT TO commands form the delivery envelope used by mail servers. Inside DATA, the email message has header lines such as From, To, and Subject, then a blank line, then the body. The visible To header and the SMTP RCPT TO command serve different purposes and need not be identical, much like a paper letter has writing inside an envelope and delivery information outside.', points: ['SMTP is defined by RFC 5321 in the source material.', 'The message-format rules are separate from SMTP transfer rules.', 'Attachments and other objects can be represented within a multipart message rather than as separate HTTP-style responses.'] },
      { title: 'Delivery is not retrieval', body: 'SMTP delivers and stores a message at the receiver\'s mail server. A mail access protocol lets the receiver manage stored mail. IMAP keeps messages on the server and supports retrieval, deletion, and folders. Webmail presents an HTTP interface to the user while its service still uses mail protocols behind the scenes. Therefore opening a browser inbox does not mean SMTP itself retrieved the displayed message.', points: ['SMTP moves mail toward storage.', 'IMAP manages mail stored on a server.', 'HTTP can provide the browser-facing webmail interface.'] },
    ],
    keyTerms: ['email', 'user agent', 'mail server', 'mailbox', 'outgoing message queue', 'SMTP', 'SMTP client', 'SMTP server', 'port 25', 'SMTP envelope', 'MAIL FROM', 'RCPT TO', 'DATA', 'persistent connection', 'mail message header', 'mail message body', 'IMAP', 'mail access protocol', 'webmail'], source: 'Chapter2-Mail-DNS.pdf pages 3-10',
  },
  {
    id: 'ch2-ftp-connections', number: '09', title: 'FTP and Separate Connections', eyebrow: 'Commands here, file bytes there', minutes: 20,
    summary: 'Explain why FTP separates control from file data, follow the chapter active-mode connection model, and interpret common commands and replies.',
    objectives: ['Describe FTP as a stateful client-server file transfer protocol', 'Distinguish the TCP control connection from a TCP data connection', 'Trace the chapter active FTP model using server ports 21 and 20 accurately', 'Match USER, PASS, LIST, RETR, and STOR to their purposes', 'Interpret representative FTP status codes and phrases'],
    sections: [
      { title: 'FTP transfers files for a client', body: 'The File Transfer Protocol lets a client upload files to or download files from a remote FTP server. The user works through an FTP client and local file system, while the server provides access to a remote file system. The side that starts the FTP session is the client even when the requested file later travels from server to client.', points: ['FTP follows a client-server architecture.', 'The chapter source identifies FTP in RFC 959.', 'Authentication and server policy determine which remote operations are allowed.'] },
      { title: 'A long-lived control connection', body: 'The FTP client opens a TCP control connection to server port 21. Authentication, directory navigation, transfer commands, and status replies travel over this connection as text. The control connection remains available while the session proceeds, and the server remembers state such as the authenticated user and current remote directory.', points: ['USER and PASS participate in authentication.', 'Commands describe what to do; they do not carry the file itself.', 'Because FTP remembers earlier login and directory choices, it is stateful.'] },
      { title: 'A separate data connection', body: 'A file listing or transfer uses a second TCP connection, so FTP control is called out-of-band relative to data. In the active-mode model shown by the chapter slides, the server opens the data connection from server port 20 to a client-provided endpoint after receiving a transfer command. It closes that data connection after one transfer and opens another for a later transfer while the port-21 control connection stays open.', points: ['Port 21 is the server\'s control port in the model.', 'Port 20 is the server-side source port for the classic active-mode data connection shown in the source.', 'Port 20 is not a universal destination for every FTP data connection; passive FTP uses a client-initiated data connection to a server-selected port.'] },
      { title: 'Commands name the operation', body: 'USER username and PASS password provide credentials over the control channel. LIST requests a listing for the current remote directory. RETR report.pdf retrieves a file from the remote host, while STOR report.pdf uploads and stores a file on the remote host. A LIST, RETR, or STOR operation needs a data connection in addition to its command on the control connection.' },
      { title: 'Replies report progress or failure', body: 'FTP replies use a status code and phrase, similar in shape to HTTP and SMTP responses. For example, 331 means the username was accepted but a password is required, and 125 says a data connection is already open and transfer is starting. A 425 reports that the data connection cannot be opened, while 452 reports an error writing the file. Reading the code together with the phase of the session helps locate the problem.' },
      { title: 'Worked connection trace', body: 'A client connects to server port 21, sends USER and PASS, changes or browses its current directory, and sends RETR notes.pdf. In the chapter\'s active model, the server then creates a separate TCP data connection from its port 20 to the endpoint supplied by the client, sends the file bytes, and closes only that data connection. The authenticated control session can remain open for another command and a new data connection.' },
    ],
    keyTerms: ['FTP', 'FTP client', 'FTP server', 'control connection', 'data connection', 'out-of-band control', 'active FTP', 'passive FTP', 'port 21', 'port 20', 'stateful protocol', 'USER', 'PASS', 'LIST', 'RETR', 'STOR', 'FTP reply code'], source: 'Chapter2-Mail-DNS.pdf pages 11-13',
  },
  {
    id: 'ch2-dns-resolution', number: '10', title: 'DNS Hierarchy, Resolution, and Caching', eyebrow: 'Turning names into addresses', minutes: 28,
    summary: 'Navigate the distributed DNS hierarchy, compare iterative and recursive work, and explain how caching improves speed while allowing temporary staleness.',
    objectives: ['Explain why DNS is a distributed application-layer database', 'Identify the roles of root, TLD, authoritative, and local DNS servers', 'Trace a hostname lookup through the DNS hierarchy', 'Differentiate iterative referrals from recursive resolution', 'Explain how TTL-based caching affects performance, load, and freshness'],
    sections: [
      { title: 'DNS gives applications usable names', body: 'People prefer names such as www.example.com, while Internet forwarding ultimately needs an IP address. The Domain Name System is both a distributed database of naming records and an application-layer protocol used to query those records. It supports hostname-to-address translation, aliases, mail-server naming, and load distribution when one name maps to multiple server addresses.', points: ['DNS is a core Internet function implemented at the application layer.', 'A name can remain familiar even when the host address changes.', 'Multiple addresses for one service name can distribute requests among replicated servers.'] },
      { title: 'Why the database is distributed', body: 'One central DNS database would concentrate failures, enormous traffic, long-distance delay, and maintenance responsibility. Instead, many organizations maintain the records for names they control. Distribution lets administration follow organizational boundaries and lets servers and caches be replicated near users, although the pieces must cooperate correctly.', points: ['DNS handles far more reads than writes.', 'Small lookup delays matter because many Internet activities begin with DNS.', 'Replication and caching improve resilience, but security and consistency remain important.'] },
      { title: 'The hierarchy delegates responsibility', body: 'Root DNS servers point resolvers toward servers for top-level domains such as .com, .org, and .edu. TLD servers point toward authoritative servers for registered domains. An authoritative DNS server provides the organization\'s official records for its names. The 13 logical root server identities are implemented by many physical instances, so the phrase does not mean only 13 machines exist.', points: ['A root server normally returns a referral rather than the final host address.', 'A TLD server knows how to continue toward a domain such as example.com.', 'An authoritative server is the source for records in the zone it serves.'] },
      { title: 'The local DNS server is the host\'s helper', body: 'A host normally sends its request to a configured local DNS server, also called a recursive resolver. This server is often operated by an ISP, organization, or public DNS provider and is not strictly another level of the root-to-authoritative hierarchy. It returns a cached answer when possible or performs the additional resolution work for the host.', points: ['The requesting application usually does not contact every hierarchy level itself.', 'The local server shields clients from repeated lookup steps.', 'A cached answer can be fast even when the authoritative server is far away.'] },
      { title: 'Iterative resolution follows referrals', body: 'Suppose the local resolver needs gaia.cs.umass.edu and has no useful cached information. It can ask a root server, receive a referral toward .edu, ask a .edu TLD server, receive a referral toward the authoritative server for umass.edu or its delegated subdomain, and ask that authoritative server for the answer. Each contacted server says, in effect, what it knows or which server should be asked next.', points: ['The local resolver performs the sequence of follow-up queries.', 'Referrals distribute work and avoid requiring upper-level servers to resolve every name fully.', 'The final answer returns through the local resolver to the requesting host.'] },
      { title: 'Recursive versus iterative work', body: 'In a recursive query, the contacted server accepts responsibility for obtaining the final answer or an error on behalf of the requester. In an iterative query, a server may return the best referral it has and let the requester continue. Hosts commonly request recursion from a local resolver, while that resolver commonly follows iterative referrals through the hierarchy. Requiring recursion at every upper-level server would place heavy work and state there.' },
      { title: 'Caching trades freshness for speed', body: 'When a DNS server learns a record or referral, it can cache it and answer later queries without repeating every hierarchy step. Each cached record has a time to live, or TTL, limiting how long it may be reused. Caching lowers response time and DNS traffic, but a changed address may not be seen everywhere until older cached entries expire, so DNS propagation is best effort rather than instantaneous.', points: ['Popular TLD referrals are often already cached, allowing the root step to be skipped.', 'TTL expiration causes a resolver to seek fresh information.', 'A longer TTL can reduce query load but can extend the lifetime of stale data.'] },
    ],
    keyTerms: ['Domain Name System', 'name resolution', 'distributed database', 'hostname', 'host aliasing', 'canonical name', 'load distribution', 'root DNS server', 'logical root server', 'TLD server', 'top-level domain', 'authoritative DNS server', 'local DNS server', 'recursive resolver', 'referral', 'iterative query', 'recursive query', 'DNS cache', 'time to live', 'TTL', 'stale record'], source: 'Chapter2-Mail-DNS.pdf pages 15-25',
  },
  {
    id: 'ch2-dns-records-security', number: '11', title: 'DNS Records, Messages, and Security', eyebrow: 'What DNS stores and trusts', minutes: 27,
    summary: 'Interpret common DNS resource records and message sections, follow domain registration data, and scope DNS attacks and DNSSEC protections carefully.',
    objectives: ['Read the name, value, type, and TTL fields of a DNS resource record', 'Distinguish A, NS, CNAME, and MX record purposes', 'Identify DNS message header fields and question, answer, authority, and additional sections', 'Explain how registrar and authoritative-server records make a domain reachable', 'Contrast DNS availability and spoofing attacks', 'State what DNSSEC authenticates and what it does not provide'],
    sections: [
      { title: 'Resource records are DNS facts', body: 'The DNS database stores resource records, often summarized as (name, value, type, TTL). The type determines how to interpret name and value, while TTL controls how long a learned copy may be cached. A resolver must therefore read the complete record rather than treating every DNS answer as a hostname-to-address pair.', points: ['An A record maps a hostname to an IPv4 address.', 'An NS record names an authoritative name server for a domain.', 'A CNAME record maps an alias to its canonical name.', 'An MX record names a mail server for a domain.'] },
      { title: 'Worked record examples', body: 'An A record for www.example.com can point to 192.0.2.10. An NS record for example.com can name dns1.example.com as an authoritative server. A CNAME can say that shop.example.com is an alias for storefront.hosting.example. An MX record for example.com can name mail.example.com as the server that should receive SMTP mail. If another hostname appears as a value, a resolver may also need its address record.', points: ['CNAME provides another name, not directly an IP address.', 'MX connects DNS to email delivery by identifying a mail host.', 'NS delegation tells resolvers where authoritative data can be found.'] },
      { title: 'Queries and replies share one message shape', body: 'A DNS query and reply use the same overall format. The header includes a 16-bit identification value, flags, and counts for later sections. A reply repeats the query identification value so the requester can match it to the outstanding query. Flags indicate facts such as query versus reply, recursion desired, recursion available, and whether a reply is authoritative.', points: ['The question section carries the requested name and type.', 'The answer section carries records that answer the question.', 'The authority section can identify authoritative servers.', 'The additional section can carry helpful records, such as an address for a named server.'] },
      { title: 'Putting a new domain into DNS', body: 'A domain owner registers a name through a registrar and supplies the names and addresses of its authoritative name servers. The registry publishes delegation information, including NS records and necessary address information for reaching those servers. The owner or DNS provider then configures the authoritative server with records such as an A record for the Web host and an MX record for mail.', points: ['Registration delegates a domain; it does not automatically invent all host records.', 'Multiple authoritative servers improve availability.', 'The authoritative data and the parent delegation must agree enough for resolvers to reach the domain.'] },
      { title: 'Availability attacks', body: 'A distributed denial-of-service attack tries to make DNS servers unavailable by overwhelming them with traffic. Root-server replication, filtering, and cached TLD information provide resilience and can let many lookups bypass a temporarily unreachable root instance. TLD or authoritative services can also be targets. Caching helps availability, but it is not a complete defense against every DDoS attack.' },
      { title: 'Spoofing and cache poisoning', body: 'An attacker may try to forge a DNS reply so a resolver accepts a bogus mapping, potentially directing users to the wrong host. If the resolver stores that false data, later clients can receive the poisoned answer until it is removed or expires. Transaction identifiers and other resolver defenses make blind forgery harder, but they do not establish cryptographic authenticity by themselves.' },
      { title: 'Scope DNSSEC carefully', body: 'DNSSEC adds digital signatures and a chain of trust so a validating resolver can authenticate the origin of signed DNS data and detect modification. It can also provide authenticated denial that a requested name or type does not exist. DNSSEC does not encrypt DNS queries or replies, hide which names are requested, guarantee server availability, prove that the named application is safe, or prevent an attacker from blocking traffic. Its protection also depends on proper signing and validation along the relevant chain.', points: ['DNSSEC addresses forged or altered DNS data, not confidentiality.', 'A valid signed address can still lead to a compromised application server.', 'DDoS remains an availability problem even when DNS responses are signed.'] },
    ],
    keyTerms: ['resource record', 'RR', 'record type', 'A record', 'NS record', 'CNAME record', 'MX record', 'DNS message', 'identification field', 'DNS flags', 'question section', 'answer section', 'authority section', 'additional section', 'authoritative answer', 'DNS registrar', 'delegation', 'glue record', 'DDoS', 'DNS spoofing', 'DNS cache poisoning', 'DNSSEC', 'digital signature', 'chain of trust', 'origin authentication', 'data integrity', 'authenticated denial'], source: 'Chapter2-Mail-DNS.pdf pages 26-30',
  },
  {
    id: 'ch2-p2p-distribution', number: '12', title: 'Distributing Files at Scale', eyebrow: 'Capacity sets the lower bound', minutes: 26,
    summary: 'Model the fastest possible client-server and peer-to-peer file distribution, identify the limiting capacity, and connect the equations to self-scalability.',
    objectives: ['Define the file-distribution variables F, N, u_s, u_i, d_i, and d_min', 'Derive the client-server distribution-time lower bound from server and receiver constraints', 'Derive the P2P lower bound from initial-copy, receiver, and aggregate-upload constraints', 'Identify which term limits a worked scenario', 'Explain why the model is a lower bound rather than a prediction of actual completion time'],
    sections: [
      { title: 'Start with one common model', body: 'A server owns a file of size F bits and must distribute it to N peers. The server can upload at u_s bits per second. Peer i can upload at u_i and download at d_i. The symbol d_min means the slowest peer download rate. The deck assumes abundant bandwidth inside the network, so only endpoint upload and download capacities limit the calculation.', points: ['F is an amount of data; every rate must use compatible bit-per-second units.', 'N counts receiving peers, not routers or connections.', 'A lower bound asks how quickly distribution could possibly finish under ideal use of the stated capacities.'] },
      { title: 'Client-server has two unavoidable limits', body: 'In client-server distribution, the server must upload N complete copies, or NF bits total. Its upload link therefore needs at least NF/u_s seconds. Every peer also must receive one file, so the slowest peer needs at least F/d_min seconds. Both conditions must be satisfied, which gives D_CS >= max(NF/u_s, F/d_min).', points: ['The server may transmit to several clients concurrently, but its aggregate upload link still must emit NF bits.', 'The max operation selects the slowest unavoidable constraint.', 'With fixed server capacity, NF/u_s grows linearly as N grows.'] },
      { title: 'P2P adds participant upload capacity', body: 'In P2P distribution, the server must inject at least one complete copy of the file, giving F/u_s. The slowest receiver still requires F/d_min. Across the swarm, peers must receive NF bits using aggregate upload capacity u_s plus the sum of every peer upload rate. The third bound is therefore NF/(u_s + sum u_i). Together, D_P2P >= max(F/u_s, F/d_min, NF/(u_s + sum u_i)).', points: ['Peers can upload pieces before they own the complete file.', 'As N grows, the numerator NF grows, but the denominator can grow too because new peers contribute upload capacity.', 'This added capacity is the quantitative reason P2P can be self-scaling.'] },
      { title: 'Read the scaling example', body: 'The deck sets every peer upload to u, F/u to one hour, server upload u_s to 10u, and d_min at least u_s. The client-server server term becomes N/10 hours: 1 hour for 10 peers, 2 hours for 20, and 3 hours for 30. The P2P aggregate term becomes N/(N + 10) hours: 0.5, about 0.667, and 0.75 hour for those same peer counts. Under this ideal model, the P2P bound approaches one hour instead of increasing without limit.' },
      { title: 'Solve the 1,000-peer exercise', body: 'Following the slide convention, 20 Gbits equals 20,480 Mbits. With N = 1,000, u_s = 30 Mbps, d_min = 2 Mbps, and every peer uploading at 2 Mbps, client-server distribution is limited by NF/u_s = 682,666.67 seconds, about 7.90 days. For P2P, F/u_s is 682.67 seconds, F/d_min is 10,240 seconds, and NF/(u_s + sum u_i) is about 10,088.67 seconds. The maximum is 10,240 seconds, or 2 hours, 50 minutes, 40 seconds.', points: ['The slowest peer download rate narrowly limits the P2P result.', 'The deck explicitly uses 1,024-based conversions; ordinary networking rate prefixes are normally decimal, so always follow the convention stated by a problem.', 'Keep extra precision during calculation and round only the final result.'] },
      { title: 'Know what the model leaves out', body: 'These expressions are ideal lower bounds, not promises about a real transfer. They omit protocol headers, latency, congestion inside the network, unavailable pieces, retransmissions, disk limits, connection limits, and peer churn. Actual completion time cannot beat the applicable lower bound and will usually be greater.', points: ['Abundant core bandwidth is a simplifying assumption.', 'A bottleneck can move when one capacity changes.', 'The formulas compare architecture-level capacity; they do not prove that every P2P implementation is faster.'] },
    ],
    keyTerms: ['file distribution', 'distribution time', 'file size F', 'peer count N', 'server upload rate', 'peer upload rate', 'minimum download rate', 'aggregate upload capacity', 'lower bound', 'bottleneck', 'self-scalability'], source: 'Chapter2-P2P.pdf pages 3-8',
  },
  {
    id: 'ch2-bittorrent-swarm', number: '13', title: 'Inside a BitTorrent Swarm', eyebrow: 'Finding peers and trading pieces', minutes: 23,
    summary: 'Follow a peer into a BitTorrent swarm, separate tracker coordination from piece traffic, and explain the classic rarest-first and upload-incentive strategies.',
    objectives: ['Distinguish a torrent, tracker, peer, neighbor, and file piece', 'Trace how a new peer discovers neighbors and begins exchanging pieces', 'Explain why rarest-first selection protects piece availability', 'Explain choking, preferred peers, and optimistic unchoking in the deck model', 'Describe how churn and modern discovery methods complicate the simplified model'],
    sections: [
      { title: 'A swarm distributes pieces, not complete copies', body: 'BitTorrent divides a file into pieces so peers can exchange useful data before completing the entire file. A torrent is the group of peers participating in that exchange. Different peers hold different subsets, so a downloader can receive one piece from one neighbor while uploading an already-held piece to another.', points: ['The deck labels its example chunks as 256 Kb; lowercase b normally means bits, while BitTorrent piece sizes are commonly stated in bytes.', 'Piece size is selected for a torrent and is not universally fixed at the deck\'s example value.', 'Simultaneous upload and download turns receivers into contributors during distribution.'] },
      { title: 'The tracker helps discovery, not bulk transfer', body: 'In the deck model, a new peer announces participation to a tracker and receives a list of other peers. It connects to a subset of them, called neighbors. The tracker carries coordination information, while file pieces travel directly among peers. A supporting server therefore does not make the bulk data path client-server.', points: ['A tracker can return a useful subset rather than every peer in the swarm.', 'Modern BitTorrent can also discover peers with distributed hash tables and peer exchange.', 'Some complete source must initially make every piece available, even though one server does not upload every final copy.'] },
      { title: 'Peers arrive, exchange, and leave', body: 'A joining peer initially has no pieces, learns what its neighbors hold, and accumulates pieces over time. Neighbor connections can change as peers disconnect or better choices appear. This churn makes management harder and can threaten availability if a scarce piece disappears with the only peer that held it. A completed peer may leave or remain to continue uploading.', points: ['A peer that remains after completion is commonly called a seeder.', 'Leaving after completion is permitted; the deck labels it selfish only as a simple incentive contrast.', 'Firewalls, address changes, and stale peer lists can make some discovered peers unreachable.'] },
      { title: 'Rarest first protects diversity', body: 'A peer periodically learns which pieces its connected neighbors advertise. Among the pieces it still needs, rarest first prioritizes the one with the fewest observed copies. Replicating scarce pieces reduces the chance that churn removes them from the local neighborhood and helps keep more exchange choices available.', points: ['Rarity is estimated from observed neighbors, not from perfect global knowledge.', 'The peer still can request only a piece offered by a reachable neighbor.', 'Rarest first is about availability; it does not determine which peer receives uploads.'] },
      { title: 'Upload choices create an incentive', body: 'In the classic behavior presented by the deck, Alice unchokes the four peers currently sending to her at the highest rates and reevaluates them every 10 seconds. Choked peers receive no regular upload from Alice. Every 30 seconds she optimistically unchokes another peer, allowing a new exchange relationship to prove useful. A successful candidate can later join the preferred group.', points: ['Reciprocal service rewards peers that contribute useful upload capacity.', 'Optimistic unchoking discovers potentially better partners and gives newcomers an opportunity.', 'Four slots and the 10/30-second timers describe the classic teaching model; modern clients may use different policies.'] },
      { title: 'Keep the architecture distinction clear', body: 'BitTorrent is neither a server-free system nor a complete mesh. Trackers or other discovery mechanisms can assist, and peers connect only to selected neighbors. Its defining distribution property is that bulk pieces move among participating end systems, allowing aggregate upload capacity to grow with the swarm.', points: ['Coordination traffic and content traffic can follow different paths.', 'P2P improves scaling opportunities but adds churn, trust, reachability, and management challenges.', 'The ideal distribution formulas describe capacity limits; rarest-first and peer selection describe how one application tries to use that capacity.'] },
    ],
    keyTerms: ['BitTorrent', 'torrent', 'swarm', 'tracker', 'peer', 'neighbor', 'piece', 'chunk', 'piece availability', 'peer churn', 'rarest first', 'choke', 'unchoke', 'optimistic unchoke', 'preferred peer', 'seeder', 'reciprocity'], source: 'Chapter2-P2P.pdf pages 9-11',
  },
]

export const chapter2QuizQuestions: QuizQuestion[] = [
  { id: 'ch2-q1', prompt: 'A new peer joins a file-sharing swarm and both downloads and uploads pieces. Which property does this illustrate?', choices: ['Self-scalability because the peer adds capacity and demand', 'A permanent-address requirement for every peer', 'Centralized scaling because one server performs every transfer', 'Statelessness because peers never retain files'], answer: 0, explanation: 'Self-scalability is the P2P tendency for aggregate service capacity to grow as participants join: the new peer requests pieces but also contributes upload bandwidth. It does not eliminate churn or management difficulties.' },
  { id: 'ch2-q2', prompt: 'A packet has reached a server host running Web and mail services. What information selects the intended receiving process?', choices: ['The URL path by itself', 'The destination IP address together with a destination port', 'The HTTP status code and phrase', 'The object size and link bandwidth'], answer: 1, explanation: 'The IP address gets traffic to the destination host, while the destination port demultiplexes it to the intended service or socket. An IP address alone cannot distinguish several processes on one host.' },
  { id: 'ch2-q3', prompt: 'Which service does TCP provide directly that UDP does not?', choices: ['A guaranteed minimum bit rate', 'Encryption of application bytes', 'Reliable, ordered byte delivery', 'A guaranteed upper bound on delay'], answer: 2, explanation: 'TCP retransmits losses, suppresses duplicates, and delivers bytes in order. Neither TCP nor UDP alone guarantees throughput or delay, and plain TCP does not encrypt; applications commonly add TLS for security.' },
  { id: 'ch2-q4', prompt: 'RTT is 60 ms and one object needs 20 ms of transmission time. Under the chapter\'s non-persistent HTTP model, how long does that object take?', choices: ['80 ms', '100 ms', '200 ms', '140 ms'], answer: 3, explanation: 'Non-persistent response time is 2RTT + transmission time: 2(60 ms) + 20 ms = 140 ms. The first RTT establishes TCP and the second carries the request and initial response.' },
  { id: 'ch2-q5', prompt: 'Which request correctly expresses complete replacement of /profile with the representation carried in the request?', choices: ['PUT /profile with the new representation in its body', 'HEAD /profile followed by a response status line', 'GET /profile with no target', 'POST only after the server sends 404'], answer: 0, explanation: 'PUT applies the enclosed representation to the named target and is described in this chapter as creating or completely replacing that resource. HEAD requests response metadata only, and GET retrieves a representation.' },
  { id: 'ch2-q6', prompt: 'A server returns 301 Moved Permanently. Where should the client look for the replacement URL?', choices: ['The Content-Length header', 'The Location header', 'The request method field', 'The Cookie header'], answer: 1, explanation: 'A 301 is a redirection response, and Location supplies the new URI. Content-Length describes body size; Cookie carries browser state back to a site.' },
  { id: 'ch2-q7', prompt: 'Which sequence correctly traces cookie-backed session state?', choices: ['URL, DNS cache, router table, TLS key', 'GET body, status phrase, access link, proxy port', 'Set-Cookie response, browser storage, Cookie request, server database lookup', 'Browser history, IP route, response body, TCP congestion window'], answer: 2, explanation: 'The server sets an identifier in a response, the browser stores it and returns it in a later request, and the site uses it as a database key. HTTP itself remains stateless even though the application links exchanges this way.' },
  { id: 'ch2-q8', prompt: 'Why can one third party correlate visits to two unrelated first-party sites?', choices: ['HTTP requires every site to share its database', 'A TCP port uniquely identifies a person', 'A 304 response includes the full browsing history', 'Both pages can trigger requests carrying the same third-party identifier'], answer: 3, explanation: 'When both pages embed resources from the same third party, that domain may receive its persistent cookie on both requests and associate each visit with one browser. Browser restrictions can limit this behavior, but it is not prevented by HTTP itself.' },
  { id: 'ch2-q9', prompt: 'Traffic offered to browsers is 1.50 Mbps, cache hit rate is 0.40, and access-link capacity is 1.54 Mbps. What utilization do origin misses create?', choices: ['About 0.58', 'About 0.40', 'About 0.90', 'About 0.97'], answer: 0, explanation: 'Miss fraction = 1 - 0.40 = 0.60. Miss traffic is 0.60 x 1.50 = 0.90 Mbps, so utilization is 0.90/1.54 = 0.584, or about 58%.' },
  { id: 'ch2-q10', prompt: 'Which statement best distinguishes HTTP/2 from HTTP/3 when a packet is lost?', choices: ['HTTP/2 opens one TCP connection per frame', 'HTTP/2 interleaves streams, but TCP loss can stall them all; HTTP/3 uses QUIC streams to reduce that coupling', 'HTTP/3 abandons reliable transfer for every object', 'HTTP/3 replaces status codes with conditional GET'], answer: 1, explanation: 'HTTP/2 multiplexes frames but places them in one ordered TCP stream, so missing TCP bytes can block all HTTP streams. HTTP/3 uses QUIC over UDP with independent reliable streams, reducing transport-level head-of-line blocking while retaining HTTP semantics.' },
  { id: 'ch2-q11', prompt: 'During server-to-server email delivery, which process is the SMTP client?', choices: ['The recipient user agent because it eventually reads the message', 'The sending mail server because it initiates the TCP connection', 'The receiving mailbox because it stores incoming messages', 'The DNS root server because it identifies the recipient'], answer: 1, explanation: 'Client and server describe behavior in an exchange. The sending mail server initiates the SMTP TCP connection, so its SMTP process is the client; the receiving mail server waits for that connection.' },
  { id: 'ch2-q12', prompt: 'Which item belongs to the SMTP delivery envelope rather than the email message header?', choices: ['Subject: Lab reminder', 'From: Alice', 'A blank line before the body', 'RCPT TO:<bob@example.edu>'], answer: 3, explanation: 'RCPT TO is an SMTP command used by servers to identify an envelope recipient. From, Subject, and the header-body separator are parts of the message content sent after DATA.' },
  { id: 'ch2-q13', prompt: 'In the active FTP model shown in the chapter source, what happens after the client sends RETR over the port-21 control connection?', choices: ['The server opens a separate data connection from server port 20 for the transfer', 'The file bytes replace all later commands on the control connection', 'The client sends the file to DNS port 53', 'The server closes the authenticated control connection before transferring'], answer: 0, explanation: 'Classic active FTP keeps commands on the TCP control connection to server port 21 and uses a separate data connection opened by the server from port 20. This port-20 statement describes the active model, not every passive FTP transfer.' },
  { id: 'ch2-q14', prompt: 'Why is FTP described as stateful?', choices: ['Every file must have the same size', 'It encrypts all commands automatically', 'The server remembers information such as authentication and the current directory across commands', 'Its data and control bytes share one stateless message'], answer: 2, explanation: 'The FTP server interprets later commands in light of earlier authentication and directory choices. That retained session context makes the application protocol stateful.' },
  { id: 'ch2-q15', prompt: 'A local resolver asks a root server for www.example.com and receives directions to a .com server. What kind of response is this?', choices: ['An SMTP greeting', 'An iterative referral', 'A final A record from the authoritative server', 'An FTP data connection'], answer: 1, explanation: 'In iterative resolution, a contacted server may return the next server to ask. The root referral directs the resolver toward the appropriate TLD server rather than resolving the complete hostname itself.' },
  { id: 'ch2-q16', prompt: 'A site changes its IP address, but some users briefly receive the old address. Which DNS behavior best explains this?', choices: ['Resolvers may reuse the old cached record until its TTL expires', 'Every recursive query permanently stores its first answer', 'MX records always override A records', 'DNSSEC encrypts the old address'], answer: 0, explanation: 'DNS caching improves speed and reduces load, but cached records remain reusable for their TTL. Different resolvers can therefore learn the updated record at different times.' },
  { id: 'ch2-q17', prompt: 'Which DNS record correctly matches its purpose?', choices: ['A maps a domain to its SMTP mail server', 'NS maps a hostname directly to an IPv4 address', 'CNAME maps an alias name to its canonical name', 'MX proves that a DNS reply is cryptographically signed'], answer: 2, explanation: 'A CNAME names the canonical target for an alias. An A record maps a hostname to an IPv4 address, NS names an authoritative server, and MX names a mail server.' },
  { id: 'ch2-q18', prompt: 'What does DNSSEC provide when signed data is properly validated?', choices: ['Confidentiality for every queried domain name', 'Guaranteed DNS availability during a DDoS attack', 'Proof that the destination application contains no malware', 'Origin authentication and integrity for DNS data'], answer: 3, explanation: 'DNSSEC signatures let a validator authenticate signed DNS data and detect alteration. DNSSEC does not encrypt queries, guarantee availability, or certify the safety of the application reached through an authenticated address.' },
  { id: 'ch2-q19', prompt: 'Why does client-server file distribution include the lower-bound term NF/u_s?', choices: ['Every peer must upload N copies', 'The server must emit N complete file copies through its aggregate upload capacity', 'The slowest peer must download N files', 'The network core stores NF bits permanently'], answer: 1, explanation: 'The server is the only source of each client-server copy. It must upload NF bits total through capacity u_s, whether it serves clients sequentially or concurrently.' },
  { id: 'ch2-q20', prompt: 'For P2P distribution, what capacity appears under NF in the aggregate-transfer bound?', choices: ['Only the slowest peer download rate', 'Only the server upload rate', 'The server upload plus the sum of peer uploads', 'The fastest peer download multiplied by N'], answer: 2, explanation: 'The swarm can use server upload and peer upload together, so the modeled aggregate capacity is u_s + sum(u_i). This growing denominator expresses self-scalability.' },
  { id: 'ch2-q21', prompt: 'Why does BitTorrent use rarest-first piece selection?', choices: ['To send every request through the tracker', 'To prioritize scarce observed pieces and preserve their availability', 'To guarantee that the fastest neighbor receives every upload', 'To replace transport-layer reliability'], answer: 1, explanation: 'Rarest first replicates pieces with few observed copies before churn can remove them from a peer\'s neighborhood. It concerns piece availability, not tracker routing or transport reliability.' },
  { id: 'ch2-q22', prompt: 'What is the purpose of optimistic unchoking in the classic BitTorrent model?', choices: ['Encrypt a randomly chosen piece', 'Delete inactive peers from the tracker', 'Try a new upload partner that may become a useful reciprocal peer', 'Guarantee that four peers always hold the complete file'], answer: 2, explanation: 'An optimistic unchoke temporarily serves another peer so a potentially better exchange relationship can be discovered and newcomers have an opportunity to participate.' },
]

export const chapter2Glossary: Array<[string, string]> = [
  ['Application architecture', 'The organization of application processes and their roles, such as client-server, peer-to-peer, or a hybrid.'],
  ['Application-layer protocol', 'Rules defining message types, syntax, field meaning, and the timing and response behavior of application processes.'],
  ['Cache hit rate', 'The fraction of requests satisfied from cached representations; the miss rate is one minus this value.'],
  ['Client process', 'The process that initiates communication in a particular exchange.'],
  ['Client-server architecture', 'An organization in which intermittently connected clients contact a reachable server service rather than one another.'],
  ['Conditional GET', 'A GET carrying a validator so the server can return 304 without an object body when the cached copy is still current.'],
  ['Congestion control', 'Adjustment of sending behavior to avoid or respond to overload in the network.'],
  ['Cookie', 'A browser-managed name-value item scoped to a site and returned in matching HTTP requests.'],
  ['CRLF', 'Carriage return followed by line feed, used to terminate HTTP/1.x lines; a blank CRLF line ends the header section.'],
  ['Entity body', 'The optional content area of a message, such as submitted form data or a representation uploaded with PUT.'],
  ['Flow control', 'Protection that keeps a fast sender from overflowing a receiving endpoint\'s available buffer.'],
  ['Head-of-line blocking', 'Delay caused when data that could otherwise proceed must wait behind earlier data.'],
  ['HTTP', 'The stateless application-layer request-response protocol used to transfer Web representations and metadata.'],
  ['HTTP/2', 'HTTP framing that multiplexes multiple streams over one TCP connection while retaining HTTP semantics.'],
  ['HTTP/3', 'HTTP carried over QUIC, with integrated security and streams that reduce transport-level head-of-line blocking.'],
  ['Interoperability', 'The ability of independently implemented systems to communicate by following the same specification.'],
  ['Non-persistent HTTP', 'An HTTP connection strategy in which a TCP connection carries at most one object.'],
  ['Origin server', 'The authoritative server from which a cache obtains a representation on a miss or validation.'],
  ['Peer-to-peer architecture', 'An organization in which end systems directly request and provide service, commonly without one always-on data server.'],
  ['Aggregate upload capacity', 'The combined upload rate available from the source server and participating peers in the ideal P2P distribution model.'],
  ['BitTorrent', 'A P2P file-distribution protocol in which peers exchange pieces with selected neighbors in a torrent.'],
  ['Choke', 'A BitTorrent peer-selection state in which one peer does not currently upload regular piece data to another.'],
  ['Distribution time', 'The elapsed time required to deliver a complete file to every intended receiver.'],
  ['Lower bound', 'A limit showing that completion cannot be faster than the largest unavoidable capacity constraint.'],
  ['Neighbor', 'A peer to which a BitTorrent participant currently maintains a connection for availability information and piece exchange.'],
  ['Optimistic unchoke', 'A periodic trial upload to a peer outside the preferred set, used to discover a potentially useful exchange partner.'],
  ['Rarest first', 'A piece-selection policy that prioritizes a needed piece with the fewest observed copies among connected neighbors.'],
  ['Persistent HTTP', 'An HTTP connection strategy that reuses one connection for multiple exchanges with the same server.'],
  ['Port number', 'A transport-layer number that helps direct received data to the intended process or socket on a host.'],
  ['QUIC', 'A secure UDP-based transport used by HTTP/3 that provides multiplexed reliable streams.'],
  ['Round-trip time', 'The elapsed time for a small packet to travel from one endpoint to another and for a response to return.'],
  ['Self-scalability', 'The P2P property in which joining peers add service capacity as well as service demand.'],
  ['Swarm', 'The peers participating in the distribution of a particular torrent.'],
  ['Torrent', 'In the chapter model, the group of peers exchanging pieces of a particular file or collection.'],
  ['Tracker', 'A BitTorrent coordination service that helps participating peers discover one another without carrying the bulk file pieces.'],
  ['Unchoke', 'A BitTorrent peer-selection state that permits piece uploads to another peer.'],
  ['Socket', 'The application-facing operating-system interface used by a process to send and receive through transport services.'],
  ['Stateless protocol', 'A protocol whose requests can be interpreted without protocol memory of earlier requests.'],
  ['Status code', 'A three-digit value in an HTTP response status line that communicates the outcome category and specific result.'],
  ['Transport Layer Security', 'A security protocol used by applications to provide confidentiality, integrity, and endpoint authentication.'],
  ['Web cache', 'An intermediary server that returns stored responses on hits and acts as a client to an origin on misses.'],
  ['Web object', 'A separately addressable resource, such as HTML, an image, a script, or media, identified by a URL.'],
  ['A record', 'A DNS resource record that maps a hostname to an IPv4 address.'],
  ['Active FTP', 'The FTP mode in which the server initiates a data connection to a client-provided endpoint, classically using server source port 20.'],
  ['Authoritative DNS server', 'A DNS server that provides official records for the zone it serves.'],
  ['CNAME record', 'A DNS resource record that maps an alias name to its canonical name.'],
  ['Control connection', 'FTP\'s TCP connection to server port 21 used for commands, replies, authentication, and session state.'],
  ['Data connection', 'A separate FTP TCP connection used to carry a directory listing or file contents for one transfer.'],
  ['DNS cache', 'Temporarily stored DNS records or referrals that let a resolver answer without repeating every lookup step.'],
  ['DNS cache poisoning', 'Placement of forged DNS data into a resolver cache so later clients receive a false answer.'],
  ['DNS registrar', 'An organization through which a registrant arranges a domain registration and supplies delegation information.'],
  ['DNSSEC', 'DNS security extensions that use signatures and a chain of trust to authenticate signed DNS data and detect modification; they do not encrypt DNS or guarantee availability.'],
  ['Domain Name System', 'The distributed hierarchical database and application-layer protocol used to resolve names and publish other naming information.'],
  ['FTP', 'A stateful file transfer protocol that separates a command-and-reply control connection from data connections.'],
  ['IMAP', 'A mail access protocol that lets a user agent retrieve and organize messages stored on a mail server.'],
  ['Iterative query', 'A DNS query for which the server may return an answer or a referral telling the requester which server to contact next.'],
  ['Local DNS server', 'The resolver a host normally queries; it answers from cache or performs resolution through the DNS hierarchy.'],
  ['Mail access protocol', 'A protocol, such as IMAP, used by a user agent to retrieve or manage messages stored on a mail server.'],
  ['Mailbox', 'Server-side storage containing incoming messages delivered for a particular email user.'],
  ['MX record', 'A DNS resource record naming a mail server associated with a domain.'],
  ['NS record', 'A DNS resource record naming an authoritative name server for a domain.'],
  ['Outgoing message queue', 'Mail-server storage for messages waiting to be sent or retried.'],
  ['Recursive query', 'A DNS request asking the contacted resolver to obtain the final answer or an error on the requester\'s behalf.'],
  ['Referral', 'A DNS response that directs a resolver to another name server closer to authoritative information.'],
  ['Resource record', 'A DNS database entry commonly represented by name, value, type, and time to live.'],
  ['Root DNS server', 'A replicated server for the DNS root that directs resolvers toward the appropriate top-level-domain servers.'],
  ['SMTP', 'The TCP-based command-response protocol used to push email toward a receiving mail server.'],
  ['SMTP envelope', 'The delivery information supplied by SMTP commands such as MAIL FROM and RCPT TO, distinct from headers inside the message.'],
  ['Time to live', 'The DNS record value that limits how long a cached copy may be reused before it must be refreshed.'],
  ['TLD server', 'A DNS server responsible for a top-level domain and referrals toward authoritative domain servers.'],
  ['User agent', 'An email application used to compose, submit, retrieve, and read messages.'],
]

export const chapter2ActivityIds = [
  'ch2-transport-match',
  'ch2-http-exchange',
  'ch2-cache-calculator',
  'ch2-smtp-flow',
  'ch2-ftp-connections',
  'ch2-dns-resolution',
  'ch2-dns-records-security',
  'ch2-p2p-distribution',
  'ch2-bittorrent-strategy',
] as const
